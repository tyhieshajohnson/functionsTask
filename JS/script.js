// Create a function called "greet" that accepts a single parameter. 
// Log the parameter along with a "Hello".
function greet(name) {
    console.log(`Hello ${name}`);
}
greet('Tyhiesha');
// Create a function called multiply. It should accept two parameters and log the multiplication of the two given parameters.
function multiply(x,y){
    let answer= x*y;
    console.log(multiply);
    return x*y;
}
multiply(6,7);
// Create a function called tableOfFive which accepts a single parameter.
// Return the parameter multiplied by five to a variable, and log this variable.
function tableOfFive(value) {
    let answer= value * 5;
    console.log(answer);
    return answer;
}
// Please take note of the difference between log and return.